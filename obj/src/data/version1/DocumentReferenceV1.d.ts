import { IStringIdentifiable } from 'pip-services-commons-node';
export declare class DocumentReferenceV1 implements IStringIdentifiable {
    constructor(id: string, name?: string);
    id: string;
    name?: string;
}
